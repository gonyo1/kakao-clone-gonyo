const playstatusBar = document.querySelector(".playstatus_bar")

function onPlaystatusClick() {
    location.href = "[✅] viewdetail.html"
}

playstatusBar.addEventListener("click", onPlaystatusClick);
